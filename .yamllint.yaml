extends: default
ignore: |
  **/tests/fixtures/**
  .venv/
rules:
  document-start: disable
  line-length:
    max: 80
    level: error
  truthy:
    check-keys: false
  comments:
    # yamlfmt owns inline comment spacing and normalizes it to one space.
    min-spaces-from-content: 1
